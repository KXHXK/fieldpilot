# 技术架构

按照第十三章“智能旅行助手”的四层结构：

1. 前端层：Vue3 + TypeScript，负责用户交互和数据展示。
2. 后端层：FastAPI，负责 API 路由、数据验证和业务编排。
3. 智能体层：HelloAgents，多 Agent 协作完成任务分解、工具调用和结果整合。
4. 外部服务层：高德地图 API、Unsplash API、LLM API 等。

目标数据流：

```text
用户填写表单
-> 后端验证数据
-> 调用智能体系统
-> 景点搜索 / 天气查询 / 酒店推荐 / 行程规划 Agent 协作
-> Agent 通过 MCP 或服务封装调用外部 API
-> 整合结果
-> 返回前端
-> 前端渲染行程、预算、地图、天气和每日详情
```

